services:
  front:
    image: juaneliezer13/baifa-ui:latest
    container_name: baifa-ui
    restart: unless-stopped
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3000
